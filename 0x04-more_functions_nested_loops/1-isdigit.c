#include "main.h"

/**
 * _isdigit - checks for a digit
 * @z: number to be checked
 *
 * Return: 1 if character is digit and 0 otherwise
 */

int _isdigit(int c);
{
	if (z >= 48 && z <= 57)
	{
		return (1);
	}
	return (0);
}
